# AI Agent Team Topology

_Started 2026-04-23 07:27 UTC_

---

## User

Create wireframe explorations for an 8-role AI agent team topology diagram.
Show 3-5 distinct layout approaches so I can compare grouping axes.

ROLES (one-line essence — do not paraphrase the role itself, only propose visual groupings):
- Master: human decision-maker, authority, sits outside the agent team
- Ace: orchestrator, decides role call order, synthesizes, speaks first and last
- Arki: structural/architectural analysis, dependencies, design constraints
- Fin: cost, resource, return evaluation, financial audit
- Riki: risk, failure modes, assumption audit, adversarial review
- Nova: OPTIONAL speculative advisor, only invoked on explicit request, never authoritative
- Dev: implementation, actual code/script execution
- Vera: visual design spec (color/typography/component), hands spec to Edi
- Edi: artifact compilation and output formatting only, no independent judgment

CANONICAL SPEAKING ORDER (for reference, not a layout rule):
Ace(framing) → Arki → Fin → Riki → [Nova if invoked] → Ace(synthesis) → Edi
Vera is called by Ace when visual work is needed, outside the main debate loop.

REQUIREMENTS:
- Master at top, separate from the agent cluster
- Ace clearly central to the agent team
- Nova must be visually distinguishable as optional/speculative (dashed, faded, detached — designer's choice)
- Edi as terminal output node
- Show the speaking-order flow AS WELL AS any grouping you propose
- Minimal labels, dark theme, b&w + one accent color

Give me 3-5 distinctly different grouping axes (e.g., by phase, by authority level, by input/output role, by invocation frequency) — do not commit to one. Side-by-side comparison.

## Assistant

I'll create wireframe explorations for the 8-role AI agent topology. Let me think through the design space first.

**Aesthetic commitment:** Dark theme (near-black #0a0a0a bg, off-white #e8e8e6 fg), one cyan accent (hue 205 per tweak), monospace labels with handwritten-sketchy feel via Caveat/Kalam for annotations. Low-fi wireframe vibe — simple shapes, dashed strokes, minimal ornamentation.

**Grouping axes I'll explore (5 distinct approaches):**
1. **By phase** — Intake → Debate → Synthesis → Output (horizontal flow)
2. **By authority level** — Human / Orchestrator / Advisors / Executors (vertical tiers)
3. **By invocation frequency** — Always / On-demand (concentric rings around Ace)
4. **By I/O role** — Inputs (critics) vs Outputs (builders) as mirrored columns
5. **By debate loop** — Canonical speaking order as a clock/cycle diagram

Let me build this as side-by-side wireframes with tweaks for accent hue.

_[tool: update_todos]_

_[tool: write_file]_

_[tool: done]_

_[tool: snip]_

_[tool: fork_verifier_agent]_

Delivered **Agent Topology.html** — 5 distinct grouping axes side-by-side + a decision-matrix card:

- **A · Phase pipeline** (intake→debate→synthesis→output, L→R)
- **B · Authority tiers** (Master / Ace / advisors / executors)
- **C · Concentric rings** (always-on inner, conditional outer)
- **D · Critics vs builders** (mirrored around Ace)
- **E · Debate loop** (clock; Ace at 12 opens + closes)

Dark theme, one cyan accent (hue tweakable, currently 205), Kalam/Caveat for sketchy labels, JetBrains Mono for role names. Master sits detached at top in every layout; Nova is always dashed/dimmed; Edi always accent-outlined as terminal. Speaking order numbered ①–⑦ where relevant.

Tweaks panel lets you shift accent hue and focus any single option fullscreen for closer comparison. Tell me which axis to push to higher fidelity (or I'd hybridize E-loop inside B-tiers).

